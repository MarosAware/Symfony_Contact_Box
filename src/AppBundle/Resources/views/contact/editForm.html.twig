{% extends "base.html.twig" %}


{% block body %}

    <div class="container">
        <h1>Edit Contact</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-info" type="submit" value="Edit">
        {{ form_end(form) }}

        {{ render(controller('AppBundle:Address:add', {'id': id})) }}
        {{ render(controller('AppBundle:Phone:add', {'id': id})) }}
        {{ render(controller('AppBundle:Email:add', {'id': id})) }}
    </div>

{% endblock %}
{% extends "base.html.twig" %}

{% block body %}

    <div class="container">
        <h1>Create new contact</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-primary" type="submit" value="Create">
        {{ form_end(form) }}
    </div>

{% endblock %}